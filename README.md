# LP-Solver-Analysis
Analyzing the runtime and number of iterations of SciPy Linear Program Solvers on small Klee-Minty Cubes

## Usage
 Run with `python3 LPSolverAnalysis.py {n}`
 Where n is the number of trials, each solver will solve each LP n times.

### Example
`python3 LPSolverAnalysis 50`